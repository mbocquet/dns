---
# defaults file for dns
dns_packages:
  - 'bind9'

# default values
dns_default_file: '/etc/default/bind9'
dns_user: 'bind'
dns_chroot: '/var/lib/named'
# dns_domain: 'example.org'
# dns_role: should be 'master' or 'slave'
# dns_role: 'master'
...
